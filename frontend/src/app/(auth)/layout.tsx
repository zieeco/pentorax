/**
 * Auth Layout
 * Full-screen layout for authentication pages — no header, no sidebar.
 */
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
